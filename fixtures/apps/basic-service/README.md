# Basic Service Fixture

Black-box acceptance fixture for Meteorite core HTTP behavior.

Covered by `fixtures/tests/basic-service.sh`; exercises Zig handlers, route methods, typed params, pattern validators, query validators, body limits, memory reports, generated LuaLS aids, capability metadata, and diagnostic failures.
