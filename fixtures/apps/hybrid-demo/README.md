# Hybrid Demo Fixture

Hybrid authoring fixture for inline Lua handlers and graph-visible capabilities.

Used by CLI invoke tests to exercise Lua handlers, HTTP/auth/Zig capability stubs, typed params, and request/response behavior without starting the Zig server.
