# Bench Service Fixture

Benchmark-only Meteorite app consumed by `bench/run.sh` and related benchmark scripts.

It keeps `/__bench/*` and Lua-state stress routes out of showcase and acceptance fixtures. Do not add product demos here; add only routes that benchmark scripts consume.
