# Static Site Fixture

Static release fixture for `m.file`, `m.dir`, and `m.site`.

The fixture intentionally includes source static assets under `site/dist`; generated release output belongs under `dist/` and must not be committed.
