# Showcase Service Fixture

Default local Meteorite app used by root `moon run graph`, `moon run dev`, and `zig build` defaults.

It demonstrates typed routes, body limits, validators, Zig handlers, graph-visible capabilities, and a single hybrid-inline route without carrying benchmark-only endpoints.
