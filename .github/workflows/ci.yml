name: Meteorite CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    name: Validate Meteorite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: moonstone-sh/moonstone
          # Includes `moon artifact create`, required by Ballad registry export.
          ref: 8594a61de69798fd534d7d9e9c595f7f25ddb3f5
          path: .ci/moonstone
      - uses: actions/checkout@v4
        with:
          repository: moonstone-sh/ballad
          path: .ci/ballad
          fetch-depth: 0
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - name: Install release tooling
        run: |
          sudo apt-get update
          sudo apt-get install -y b3sum file jq libsqlite3-dev lsof luajit pkg-config zstd
      - name: Build Moonstone CLI
        working-directory: .ci/moonstone
        run: |
          zig build
          echo "$GITHUB_WORKSPACE/.ci/moonstone/zig-out/bin" >> "$GITHUB_PATH"
      - name: Verify Moonstone artifact export support
        run: moon artifact create --help
      - name: Synchronize project lockfile
        run: |
          export SQLITE_INCDIR="$(pkg-config --variable=includedir sqlite3)"
          export SQLITE_LIBDIR="$(pkg-config --variable=libdir sqlite3)"
          test -f "$SQLITE_INCDIR/sqlite3.h"
          moon sync
      - name: Prepare locked Ballad integration checkout
        run: |
          ballad_version="$(awk '
            /^\[\[package\]\]$/ { in_ballad = 0; next }
            $0 == "name = \"moonstone/ballad\"" { in_ballad = 1; next }
            in_ballad && /^version = / { value = $0; sub(/^version = "/, "", value); sub(/"$/, "", value); print value; exit }
          ' moonstone.lock)"
          test -n "$ballad_version"
          git -C .ci/ballad checkout --detach "v$ballad_version"
          ln -s "$GITHUB_WORKSPACE/.ci/ballad" "$GITHUB_WORKSPACE/../ballad"
          cd .ci/ballad
          moon sync --locked
      - name: Validate release gate
        run: moon run test-release-gate
      - name: Export release candidate with locked Ballad tool
        run: moon exec ballad play partiture.lua
