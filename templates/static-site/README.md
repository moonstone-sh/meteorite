# {{name}}

A Meteorite static-site starter using `m.site()` for HTML fallbacks, immutable assets, compressed variants, and explicit JSON files.

## Try it

```bash
moon sync
moon run graph
moon run build
moon run run
```

Routes are declared in Lua at build time, while files are served by the compiled Meteorite server.
